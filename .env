# Environment variables declared in this file are available at build time and runtime.
# Only put secrets in this file. Public variables should go in next.config.js or be prefixed with NEXT_PUBLIC_

# Database - SQLite (no installation required)
DATABASE_URL="file:./dev.db"
